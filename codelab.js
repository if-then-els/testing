document.addEventListener("DOMContentLoaded", function () {
    loadAppointments();
    loadExplanations();
});

function loadAppointments() {
    // Use AJAX to fetch appointments from PHP script
    // Update the appointments table in the HTML
}

function loadExplanations() {
    // Use AJAX to fetch patient explanations from PHP script
    // Update the patient explanations section in the HTML
}
